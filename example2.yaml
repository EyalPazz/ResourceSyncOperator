apiVersion: sync.resourcesyncoperator.com/v1alpha1
kind: ResourceSync
metadata:
  name: example2
spec:
  route:
    name: admin-client-route
    namespace: heroes-live-on-production
  secret:
    name: heroes-tls
    namespace: cert-manager
