apiVersion: sync.resourcesyncoperator.com/v1alpha1
kind: ResourceSync
metadata:
  name: example
spec:
  route:
    name: superadmin-client-route
    namespace: after-me-staging
  secret:
    name: heroes-tls
    namespace: cert-manager
